[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rec-forge"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["core"]
package-dir = {"" = "src"}